<?php
// public/results.php
session_start();
require_once __DIR__ . '/../core/config.php';
require_once __DIR__ . '/../core/database.php';
require_once __DIR__ . '/../core/auth.php';
require_once __DIR__ . '/../core/services/LottoResultService.php';

if (!isLoggedIn()) {
    header('Location: login.php');
    exit;
}

// Initialize the results engine container
$resultService = new LottoResultService($pdo);
$yesterdayResult = $resultService->getYesterdayResult();
$historicalLogs = $resultService->getHistoricalResults(15);

$pageTitle = 'Draw Outcome Audits';
require_once __DIR__ . '/pages/header.php';
?>

<div class="max-w-7xl mx-auto px-4 py-4 md:py-6 space-y-6 md:space-y-8">
    
    <!-- Title Section -->
    <div class="border-b border-white/5 pb-4 md:pb-6">
        <nav class="flex items-center gap-2 text-[10px] md:text-xs font-bold uppercase tracking-widest text-slate-500 mb-1.5 md:mb-2">
            <a href="lotto.php" class="hover:text-purple-400 transition-colors">Sovereign Terminal</a>
            <i class="bx bx-chevron-right text-sm"></i>
            <span class="text-slate-400">Winning Assertions</span>
        </nav>
        <h1 class="text-2xl md:text-3xl font-black text-white tracking-tight">Outcome Metrics & Archives</h1>
        <p class="text-xs md:text-sm text-slate-400 mt-1">Validated consensus arrays generated by system draw processes.</p>
    </div>

    <!-- PRIMARY TARGET: Yesterday's Winning Frame Hero Card -->
    <div class="glass-card bg-gradient-to-br from-purple-950/20 via-slate-900 to-slate-950 border border-purple-500/30 shadow-2xl p-5 md:p-8 rounded-2xl relative overflow-hidden">
        <div class="absolute top-0 right-0 w-64 h-64 bg-purple-500/5 rounded-full blur-3xl pointer-events-none"></div>
        
        <div class="flex flex-col md:flex-row md:items-center justify-between gap-6 relative z-10">
            <div class="space-y-1">
                <span class="inline-flex items-center gap-1.5 text-[10px] uppercase font-black tracking-widest text-purple-400 bg-purple-500/10 px-2.5 py-1 rounded-md border border-purple-500/20">
                    <span class="w-1.5 h-1.5 rounded-full bg-purple-400 animate-pulse"></span> Yesterday's Draw Sequence
                </span>
                <h2 class="text-xl md:text-2xl font-black text-white pt-1">Yesterday's Confirmed Result</h2>
                <p class="text-xs text-slate-400 font-mono">
                    <?php echo $yesterdayResult ? date('F d, Y', strtotime($yesterdayResult['draw_date'])) : date('F d, Y', strtotime('yesterday')); ?>
                </p>
            </div>

            <!-- The Output Matrix Node -->
            <div>
                <?php if ($yesterdayResult && !empty($yesterdayResult['winning_sequence'])): ?>
                    <div class="flex gap-2 font-mono font-black tracking-widest">
                        <?php 
                        $digits = str_split($yesterdayResult['winning_sequence']);
                        foreach($digits as $digit): ?>
                            <span class="inline-flex items-center justify-center bg-slate-950 text-white border-2 border-purple-500/50 shadow-[0_0_15px_rgba(147,51,234,0.2)] w-10 h-12 md:w-14 md:h-16 text-lg md:text-2xl rounded-xl">
                                <?php echo htmlspecialchars($digit, ENT_QUOTES, 'UTF-8'); ?>
                            </span>
                        <?php endforeach; ?>
                    </div>
                <?php else: ?>
                    <div class="text-slate-500 font-mono text-xs md:text-sm bg-slate-950/50 px-4 py-3 rounded-xl border border-white/5">
                        <i class="bx bx-time-five text-amber-500 mr-1.5"></i> Matrix array generation pending system verification.
                    </div>
                <?php endif; ?>
            </div>
        </div>

        <?php if ($yesterdayResult): ?>
            <!-- Metric Metadata Sub-Block -->
            <div class="grid grid-cols-2 sm:grid-cols-4 gap-4 mt-6 pt-6 border-t border-white/5 font-mono">
                <div>
                    <span class="block text-[9px] uppercase tracking-wider text-slate-500 font-bold">Total Network Payout</span>
                    <span class="text-sm font-bold text-emerald-400">₦<?php echo number_format($yesterdayResult['total_prize_pool'] ?? 0, 2); ?></span>
                </div>
                <div>
                    <span class="block text-[9px] uppercase tracking-wider text-slate-500 font-bold">Successful Claims</span>
                    <span class="text-sm font-bold text-slate-200"><?php echo number_format($yesterdayResult['total_winners'] ?? 0); ?> Nodes</span>
                </div>
            </div>
        <?php endif; ?>
    </div>

    <!-- SUB-LEVEL ARCHIVE: Extended Timeline Log -->
    <div class="space-y-4">
        <h3 class="text-sm font-black uppercase text-slate-400 tracking-wider">Historical Outcome Registers</h3>
        
        <div class="glass-card overflow-hidden shadow-2xl">
            <?php if (!empty($historicalLogs)): ?>
                <!-- DESKTOP HISTORICAL TABLE -->
                <div class="hidden md:block overflow-x-auto">
                    <table class="w-full text-left border-collapse">
                        <thead>
                            <tr class="text-[10px] uppercase tracking-widest text-slate-400 bg-slate-950/70 border-b border-white/5">
                                <th class="px-6 py-4">Verification Date</th>
                                <th class="px-6 py-4">Validated Result String</th>
                                <th class="px-6 py-4">Total System Distribution</th>
                                <th class="px-6 py-4 text-right">Settled Multi-Nodes</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-white/5">
                            <?php foreach ($historicalLogs as $row): ?>
                                <tr class="hover:bg-white/[0.01] transition-colors <?php echo ($yesterdayResult && $row['draw_date'] === $yesterdayResult['draw_date']) ? 'bg-purple-500/[0.02]' : ''; ?>">
                                    <td class="px-6 py-4 text-slate-300 text-xs font-semibold font-mono">
                                        <?php echo date('Y-m-d (D)', strtotime($row['draw_date'])); ?>
                                    </td>
                                    <td class="px-6 py-4">
                                        <div class="flex gap-1 font-mono font-bold text-white tracking-widest">
                                            <?php 
                                            $historicalDigits = str_split($row['winning_sequence']);
                                            foreach($historicalDigits as $digit): ?>
                                                <span class="inline-block bg-slate-950 border border-white/10 px-2 py-0.5 rounded text-sm font-black"><?php echo htmlspecialchars($digit, ENT_QUOTES, 'UTF-8'); ?></span>
                                            <?php endforeach; ?>
                                        </div>
                                    </td>
                                    <td class="px-6 py-4 text-emerald-400 font-mono text-sm font-semibold">
                                        ₦<?php echo number_format($row['total_prize_pool'] ?? 0, 2); ?>
                                    </td>
                                    <td class="px-6 py-4 text-right font-mono text-xs text-slate-400">
                                        <?php echo number_format($row['total_winners'] ?? 0); ?> matches
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>

                <!-- MOBILE HISTORICAL CARD SHIFT -->
                <div class="block md:hidden divide-y divide-white/5">
                    <?php foreach ($historicalLogs as $row): ?>
                        <div class="p-4 space-y-3 bg-slate-900/40 hover:bg-slate-900/60 transition-colors">
                            <div class="flex justify-between items-center">
                                <span class="text-xs font-mono font-black text-slate-300">
                                    <?php echo date('M d, Y (D)', strtotime($row['draw_date'])); ?>
                                </span>
                                <span class="text-[9px] font-mono text-slate-500 uppercase">
                                    <?php echo number_format($row['total_winners'] ?? 0); ?> Winners
                                </span>
                            </div>

                            <div class="flex justify-between items-center">
                                <div class="flex gap-1 font-mono font-black text-white tracking-widest">
                                    <?php 
                                    $historicalDigits = str_split($row['winning_sequence']);
                                    foreach($historicalDigits as $digit): ?>
                                        <span class="inline-block bg-slate-950 border border-white/5 px-2 py-0.5 rounded text-xs"><?php echo htmlspecialchars($digit, ENT_QUOTES, 'UTF-8'); ?></span>
                                    <?php endforeach; ?>
                                </div>
                                <div class="text-right text-emerald-400 font-mono text-xs font-semibold">
                                    ₦<?php echo number_format($row['total_prize_pool'] ?? 0, 2); ?>
                                </div>
                            </div>
                        </div>
                    <?php endforeach; ?>
                </div>
            <?php else: ?>
                <div class="px-6 py-16 text-center text-sm text-slate-500 font-medium bg-slate-950/10">
                    <i class="bx bx-history text-3xl block text-slate-600 mb-2"></i>
                    No archived database draw records compiled inside this terminal frame.
                </div>
            <?php endif; ?>
        </div>
    </div>
</div>

<?php require_once __DIR__ . '/pages/footer.php'; ?>